# Let's alarm — 프로젝트 잠재적 버그 및 UX 개선 TODO 리스트

이 문서는 앱 시연 및 최종 제출 전후로 보완해야 할 기능적 부자연스러움, 잠재적 버그 요인, 그리고 UX(사용자 경험) 향상을 위한 개선 항목들을 정리한 TODO 리스트입니다.

---

## 📅 [ ] 1. 요일 반복 알람과 스누즈(Snooze)의 예약 배타성 개선 (우선순위: 높음)
- [ ] **문제 분석**: `AlarmNotificationService`에서 알람이 스누즈 상태(`snoozeUntil != null`)가 되면 기존 요일 반복 예약이 취소되고 일회성 스누즈 예약만 활성화되는 문제 해결
- [ ] **TODO**:
  - [ ] `alarm_notification_service.dart` 내 `scheduledRequestsFromAlarm` 함수 수정
  - [ ] 스누즈 상태에서도 원래의 주간 반복 예약 목록(`repeatWeekdays` 기반)이 취소되지 않고 함께 등록되도록 로직 분리
  - [ ] 한 기기의 스누즈가 다른 기기의 주간 정기 예약을 임시 차단하지 않도록 동기화 범위 테스트

## 🔄 [ ] 2. 로그인 시 로컬 알람 데이터 마이그레이션 적용 (우선순위: 보통)
- [ ] **문제 분석**: 로컬 모드를 사용하다가 로그인 및 그룹 활성화 시 로컬 저장소의 알람 목록이 Firestore로 연동되지 않고 유실(감춰짐)되는 현상 개선
- [ ] **TODO**:
  - [ ] `AuthScreen` 또는 `GroupSetupScreen`에 로컬 데이터 마이그레이션 흐름 추가
  - [ ] 최초 그룹 연결 시 로컬 SharedPreferences에 저장되어 있던 알람 데이터들을 Firestore 해당 그룹 하위 컬렉션에 업로드(Upsert)하는 로직 구현
  - [ ] 마이그레이션 완료 후 로컬 캐시 정리 및 알람 재스케줄링

## 🔇 [ ] 3. 무음/무진동 알람 예약 방지 유효성 검사 추가 (우선순위: 보통)
- [ ] **문제 분석**: 알람 편집 시 소리와 진동을 모두 끌 수 있어, 화면만 켜지고 인지하지 못하는 오작동성 알람 예약을 허용하는 기획 오류 방지
- [ ] **TODO**:
  - [ ] `AlarmEditorSheet`에서 소리(Sound)와 진동(Vibration) 스위치가 둘 다 꺼지려고 할 때 경고 팝업 또는 스낵바 노출
  - [ ] 둘 중 최소 하나는 반드시 활성화되도록 토글 제약 조건(Validation) 구현

## 🔌 [ ] 4. 오프라인(네트워크 단절) 시 즉시 해제 및 온라인 싱크 (우선순위: 보통)
- [ ] **문제 분석**: Firebase 모드에서 기기가 네트워크 불안정 또는 단절(오프라인) 상태일 때, 알람을 꺼도 Firestore 응답 대기 및 타임아웃 때문에 화면 닫기와 소리 중지가 지연되는 현상 개선
- [ ] **TODO**:
  - [ ] 알람 해제(Dismiss) 및 스누즈(Snooze) 트리거 시 로컬 DB(SharedPreferences) 및 네이티브 동작(AlarmManager 취소, 진동 중지)을 **네트워크 상태에 관계없이 즉각 우선 적용**하도록 변경
  - [ ] 전송에 실패한 Firestore 명령은 큐(Queue)에 보관하고, 기기가 온라인 상태로 전환(`Connectivity` 감지 등)되었을 때 백그라운드에서 백엔드로 일괄 동기화하는 Offline-First 구조 설계

## 🕒 [ ] 5. 자동 스누즈 트리거 시 기기 간 시간차 경쟁 상태 해결 (우선순위: 낮음)
- [ ] **문제 분석**: 기기 A에서 5분간 알람이 울린 후 발생한 `자동 스누즈` Firestore 기록이, 아직 5분이 경과하지 않은 기기 B의 정상적인 울림까지 도중에 강제 중단시키는 부작용 해결
- [ ] **TODO**:
  - [ ] 자동 제어(Auto-Dismiss / Auto-Snooze) 동작 시 발생하는 command 페이로드에 `autoTriggered: true` 플래그 추가
  - [ ] 다른 기기에서 수신 시, 본인의 로컬 시간 기준과 대조하여 미세 오차(예: 수 초 내외)가 있을 때 오작동하지 않도록 임계치 비교 검증 로직 추가

## 👥 [ ] 6. 그룹 내 다른 사용자와의 알람 Dismiss/Snooze 동기화 범위 기획 및 대안 (우선순위: 높음)
- [ ] **문제 분석**: 다른 사용자 계정이 같은 그룹에 속해 있을 때, 한 명이 알람을 끄면 다른 멤버들의 알람까지 꺼져버려 기상 목적의 공유 알람 시 다른 사용자의 미동작(지각 등) 부작용이 우려됨.
- [ ] **TODO (기획 검토 및 개선안 적용)**:
  - [ ] **Option A (추천)**: 알람 개별 옵션 제공
    - 알람 편집 시 '개별 해제(각자 폰에서 직접 끄기)'와 '공동 해제(최초 1명이 끄면 그룹 전체 알람 멎음)' 옵션을 추가하는 기획 논의
  - [ ] **Option B**: 울림 종료 명령(Dismiss/Snooze)의 전달 계정 제한
    - 알람 설정만 동기화하되, Dismiss/Snooze 커맨드 전송 대상을 동일 계정(동일 소유자 기기군)으로만 좁히거나 FCM 연동 로직에서 액션 전파 비활성화
  - [ ] **Option C (임시 방안)**: 발표 시나리오 방어 논리 구성
    - 현재 로직을 유지하고 발표 시 '1인 멀티 디바이스 제어 편의성' 또는 '당직 및 장애 온콜 대기조의 신속한 응답 공유'에 최적화된 설계임을 명시하여 대처

## 🗺️ [ ] 7. 시스템 타임존(TimeZone) 변경 감지 시 알람 재조정 적용 (우선순위: 보통)
- [ ] **문제 분석**: 사용자가 해외 이동 등으로 디바이스의 시스템 타임존을 변경했을 때, 기동되어 있는 알람 예약들의 절대 시간이 자동으로 갱신되지 않아 엉뚱한 시각에 알람이 트리거되는 현상 방지
- [ ] **TODO**:
  - [ ] Android Native 단에 `ACTION_TIMEZONE_CHANGED` 브로드캐스트 리시버 등록
  - [ ] 타임존 변경 신호 수신 시 Flutter Engine Method Channel을 통해 `AlarmNotificationService`에 타임존 재조정 요청
  - [ ] 등록된 모든 활성 알람의 다음 울림 시간을 새로운 타임존 기준으로 재계산하여 스케줄링 재등록

## 🔋 [ ] 8. 백그라운드 구동을 위한 배터리 최적화 제외 상세 안내(UX) 보완 (우선순위: 보통)
- [ ] **문제 분석**: 안드로이드 도즈(Doze) 모드 및 제조사별 백그라운드 앱 강제 종료 정책으로 인해 백그라운드 환경에서 정시 알람이 누락되는 현상을 방지하기 위한 UX 가이드 제공
- [ ] **TODO**:
  - [ ] 앱 최초 기동 시 또는 설정 화면 내에 배터리 최적화 제외 권한 상태 표시
  - [ ] 최적화 제외가 되어 있지 않은 경우, 사용자가 시스템 설정의 '배터리 제한 없음' 탭으로 직관적으로 찾아갈 수 있도록 돕는 유려한 일러스트/애니메이션 가이드 다이얼로그 설계 및 구현

## 🔊 [ ] 9. 알람별 개별 볼륨(Volume) 제어 기능 제공 (우선순위: 낮음)
- [ ] **문제 분석**: 시스템 전체 알람 볼륨 크기에만 의존하여, 개별 알람 상황이나 성격(가벼운 알람 vs 시끄러운 알람)에 맞춰 앱 내에서 미세 볼륨 조절이 불가능한 기능적 불편함 해소
- [ ] **TODO**:
  - [ ] `Alarm` 데이터 모델에 `volume` 필드 추가
  - [ ] `AlarmEditorSheet` UI에 볼륨 슬라이더 컴포넌트 추가 및 DB 저장 연동
  - [ ] 알람 재생(Ringing) 시작 시, 지정된 알람 볼륨 값에 맞춰 임시로 오디오 볼륨을 할당하여 동작하도록 구현

## ☁️ [ ] 10. 알람 동기화 진행 상태 피드백 UI 추가 (우선순위: 낮음)
- [ ] **문제 분석**: 사용자가 알람 변경 시 해당 내역이 Firestore 서버에 성공적으로 연동되었는지 실시간 전송 상황을 시각적으로 인지하기 어려움
- [ ] **TODO**:
  - [ ] 알람 저장/토글 변경 시 즉시 완료 화면으로 넘어가기보다, 업로드 진행 중 상태일 때 알람 카드 내부나 우상단 인디케이터에 스피너(Loader) 노출
  - [ ] Firestore 업로드 완료 수신 시 로딩 상태를 해제하고 체크 애니메이션 노출

## 🚪 [ ] 11. 그룹 소유자 탈퇴 시 고아(Orphaned) 그룹 정리 시스템 구축 (우선순위: 낮음)
- [ ] **문제 분석**: 그룹을 생성한 방장(Owner)이 탈퇴하거나 그룹을 떠났을 때, 소유권 이관이나 하위 리소스(Alarms, Devices 등) 정리가 이루어지지 않아 서버 내 고아 데이터 및 토큰 찌꺼기가 영구 적체되는 문제 예방
- [ ] **TODO**:
  - [ ] 방장이 그룹을 탈퇴하거나 계정을 삭제할 때 이관할 다음 멤버 지정을 필수 요구하거나,
  - [ ] Firebase Cloud Functions 내에 그룹 멤버 수가 0이 되었을 때 해당 `groupId` 하위의 모든 alarms, devices, commands 컬렉션 및 `groupSecrets`를 자동 트래시(Trash) 정리하는 트리거 함수 추가

## 🏷️ [ ] 12. `LaunchRouterActivity` 인텐트 페이로드 키 불일치 개선 (우선순위: 매우 높음)
- [ ] **문제 분석**: 알람 수신 라우터(`LaunchRouterActivity.kt`)에서 `hasExtra("payload")`를 타겟으로 값을 검사하지만, 백그라운드 리시버(`ForegroundAlarmReceiver`)는 `EXTRA_ALARM_PAYLOAD` (`"alarm_payload"`) 문자열로 값을 넣어 보냄으로써 키 불일치 오작동이 일어남
- [ ] **TODO**:
  - [ ] `LaunchRouterActivity`의 인텐트 데이터 키 검사 조건을 `hasExtra(SyncedAlarmFlutterActivity.EXTRA_ALARM_PAYLOAD)`로 수정하여 정합성 일원화

## 🧭 [ ] 13. `LaunchRouteDecision` 내 정시 알람 액션 라우팅 우회 수정 (우선순위: 매우 높음)
- [ ] **문제 분석**: `LaunchRouteDecision.shouldRouteToAlarmActivity`가 사용자가 알림 팝업을 직접 클릭했을 때의 액션인 `"SELECT_NOTIFICATION"`만 필터링하여 `AlarmActivity`로 라우팅함. 이로 인해 백그라운드 리시버가 잠금화면 위에 다이렉트로 알람 화면을 띄우기 위해 던진 `"com.teamproject.synced_alarm.ALARM_TRIGGER"` 액션은 `MainActivity`로 우회되어 잠금화면 위 울림 화면 동작이 차단됨
- [ ] **TODO**:
  - [ ] `shouldRouteToAlarmActivity` 판단 조건에 `SyncedAlarmFlutterActivity.ACTION_ALARM_TRIGGER` 액션도 포함하여, 백그라운드 정시 기동 시 `AlarmActivity`가 강제로 실행되도록 매핑 수정

## 🔑 [ ] 14. 방장의 그룹 초대 코드(Invite Code) 재조회 UX 개선 (우선순위: 보통)
- [ ] **문제 분석**: Firestore Rules 보안 강화로 클라이언트의 `groupSecrets/{groupId}` 접근(읽기)을 전면 금지하였으나, 이로 인해 그룹 소유자(Owner)조차도 첫 개설 시 발급받은 초대 코드를 기록해두지 않으면 코드를 다시 확인할 수 없어 멤버 추가가 불가능해지는 UX 결함 발생
- [ ] **TODO**:
  - [ ] 그룹 방장(Owner) 검증을 통과한 사용자 요청에 한해, 암호화된 `groupSecrets`에서 초대 코드를 조회(또는 복원)하여 안전하게 반환해주는 전용 HTTPS Callable Cloud Function 추가 구현

## ⏳ [ ] 15. 도즈 모드 복귀 지연 대비 `DueTickTracker` 70초 타임 임계값 완화 (우선순위: 보통)
- [ ] **문제 분석**: `AlarmDueTickTracker.dart` 내 `justBecameDue` 판별 시 예정 시각보다 70초 이상 차이가 벌어지면 알람 재생을 무시함. 안드로이드 도즈 모드가 깨어나는 랙 지연으로 인해 70초 이상 늦게 앱 프로세스가 실행되면 알람 자체가 영구 누락됨
- [ ] **TODO**:
  - [ ] `justBecameDue` 시간차 검증 임계치를 5~10분 단위로 안전하게 확대하거나, 
  - [ ] 네이티브 `AlarmManager`를 거쳐 직접 호출된 정시 트리거의 경우에는 시간 임계값 체크를 우회하고 오직 틱 캐시 중복 여부만 판별하도록 로직 완화

## 💎 [ ] 16. `Alarm.fromJson` 내 `revision` 역직렬화 타입 안전성 보완 (우선순위: 보통)
- [ ] **문제 분석**: Firestore에서 `FieldValue.increment(1)`을 적용하여 알람 활성 여부 등을 업데이트할 때, 로컬 디바이스 캐시가 서버 트랜잭션 전 스냅샷 스트림을 탈 때 `revision`의 타입이 정수(`int`)가 아닌 `FieldValue` 인스턴스 그대로 들어와 `as int?` 캐스팅에서 `TypeError` 크래시를 일으킬 수 있음
- [ ] **TODO**:
  - [ ] `revision` 역직렬화 시 `as int?`를 걷어내고, 타입을 유연하게 다루어 `int` 타입으로 안전하게 변환해주는 헬퍼 래퍼 함수(`_boundedInt` 등) 적용
