# Let's alarm — 발표자료 제작용 프로젝트 정보 전달 문서

> **과목**: UI/UX프로그래밍 | **팀**: 6조 | **발표일**: 05.27(수) | **시간**: 15분 내외
>
> 이 문서는 **발표자료를 제작하는 팀원에게 전달하기 위한 목적**으로, 발표 공지의 평가 항목 순서에 맞추어 프로젝트 정보를 정리한 것입니다.

---

## (1) 프로그램 개요 및 설명 (5점)

### 프로젝트명
**Let's alarm** (렛츠알람) — 멀티 디바이스 실시간 동기화 알람 앱

### 개발 배경
- 기존 알람 앱은 **한 기기 안에서만 동작**하여, 여러 기기 간 알람 상태 공유나 해제/스누즈 제어 동기화가 불가능합니다.
- 이를 해결하기 위해 **Flutter + Firebase** 기반의 멀티 디바이스 실시간 알람 앱을 기획하였습니다.
- 먼저 **Android 앱**에서 알람 기능을 안정적으로 구현하고, 이후 Web/PC로 **확장 가능한 구조**를 목표로 합니다.

### 프로그램 설명
사용자는 Android 앱에서 알람을 생성·수정·삭제하고, 알람이 울릴 때 앱 알림을 받을 수 있습니다. 이메일 계정으로 로그인하면 **그룹**을 만들거나 초대 코드로 참가하여, 같은 그룹에 속한 **여러 기기가 동일한 알람을 실시간으로 공유**합니다. 한 기기에서 알람을 해제하거나 스누즈하면 **다른 기기에도 즉시 반영**됩니다.

### 개발 플랫폼 및 기술 스택
| 구분 | 내용 |
|---|---|
| **기본 구현 플랫폼** | Android |
| **확장 고려 플랫폼** | Web, Linux, Windows |
| **개발 프레임워크** | Flutter (Dart) |
| **상태 관리** | Riverpod (Provider 패턴) |
| **백엔드/인증** | Firebase (Authentication, Cloud Firestore, FCM, Cloud Functions) |
| **네이티브 코드** | Kotlin (Android 알람/진동/Activity 제어) |
| **IDE** | Android Studio / VS Code |

### 팀원 소개 및 역할 분담
| 팀원 | 담당 영역 |
|---|---|
| 김준형 | 계획, 디자인, 기능 구현, 테스트 |
| 양상현 | 자료조사 및 발표자료 준비, 테스트 |
| 정준호 | 코드 작성, 기능 구현, 테스트 |
| 김민준 | UI/UX 기획 및 자료조사, 요구사항 정의서 작성, 테스트 |

### 프로젝트 규모
| 항목 | 수치 |
|---|---|
| Flutter(Dart) 소스 코드 | 약 9,100줄 (26개 파일) |
| Kotlin 네이티브 코드 | 약 530줄 (8개 파일) |
| Cloud Functions (JavaScript) | 약 410줄 (1개 파일) |
| Firestore 보안 규칙 | 약 180줄 |
| 자동화 테스트 코드 | 약 1,380줄 (7개 파일, 47개 테스트 케이스) |
| Git 커밋 수 | 48개 커밋 |

---

## (2) 전체 구조도 설명 (10점)

### 2-1. 전체 시스템 구조도

```
┌─────────────────────────────────────────────────────────────────┐
│                     Firebase Cloud                              │
│                                                                 │
│   ┌─────────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│   │  Authentication  │  │  Firestore   │  │ Cloud Functions  │  │
│   │ (Email/Password) │  │  (실시간 DB) │  │ (서버리스 로직) │  │
│   └────────┬────────┘  └──────┬───────┘  └────────┬─────────┘  │
│            │                  │                    │             │
│            │                  │   알람 변경 감지    │             │
│            │                  │◄──────────────────►│             │
│            │                  │                    │             │
│            │                  │         ┌──────────┴──────────┐ │
│            │                  │         │   FCM (푸시 전송)    │ │
│            │                  │         └──────────┬──────────┘ │
└────────────┼──────────────────┼────────────────────┼────────────┘
             │                  │                    │
     ────────┼──────────────────┼────────────────────┼────────────
             │                  │                    │
    ┌────────┴──────────────────┴────────────────────┴────────┐
    │              Flutter Android 앱 (Let's alarm)           │
    │                                                         │
    │  ┌─────────────┐  ┌──────────────┐  ┌───────────────┐  │
    │  │  UI Layer    │  │ Data Layer   │  │Platform Layer │  │
    │  │  (화면/위젯) │  │ (Repository) │  │ (알림/알람)   │  │
    │  └─────────────┘  └──────────────┘  └───────────────┘  │
    │                                                         │
    │  ┌─────────────────────────────────────────────────┐    │
    │  │     Kotlin Native (AlarmManager, Vibration)     │    │
    │  └─────────────────────────────────────────────────┘    │
    └─────────────────────────────────────────────────────────┘
             │                                    │
    ┌────────┴────────┐                ┌──────────┴──────────┐
    │  Android 기기 A  │   실시간 동기화  │  Android 기기 B     │
    │  (알람 생성/해제) │◄──────────────►│ (알람 수신/동기화)   │
    └─────────────────┘                └─────────────────────┘
```

### 2-2. 동기화 흐름 설명

1. **사용자 → 앱**: 알람 생성/수정/삭제/해제/스누즈
2. **앱 → Firestore**: 변경된 알람 데이터를 클라우드에 저장
3. **Firestore → Cloud Functions**: 알람 변경 이벤트 감지 (`onAlarmWrite`, `onCommandCreate`)
4. **Cloud Functions → FCM**: 같은 그룹의 모든 기기에 데이터 메시지 전송 (500개 단위 청크 분할)
5. **FCM → 다른 기기**: 백그라운드에서도 데이터 페이로드를 수신하여 **로컬 알람 즉시 재스케줄링**
6. **Dismiss/Snooze 명령 동기화**: 한 기기에서 알람을 해제하면, `alarm.command`를 통해 다른 기기의 울림 화면도 **즉시 닫힘**

### 2-3. Firestore 데이터 구조

```
users/{uid}                          ← 사용자 프로필
users/{uid}/groups/{groupId}         ← 가입한 그룹 참조
users/{uid}/devices/{deviceId}       ← 개인 기기 목록
groups/{groupId}                     ← 그룹 정보
groups/{groupId}/members/{uid}       ← 그룹 멤버
groups/{groupId}/alarms/{alarmId}    ← 공유 알람 데이터
groups/{groupId}/devices/{deviceId}  ← 그룹 등록 기기 (FCM 토큰)
groups/{groupId}/commands/{commandId}← 해제/스누즈 명령
groupSecrets/{groupId}               ← 초대 코드 해시 (클라이언트 읽기 금지)
```

### 2-4. 앱 내부 코드 구조 (레이어드 아키텍처)

```
synced_alarm/
├── lib/
│   ├── main.dart                        ← 앱 진입점
│   ├── firebase_options.dart            ← Firebase 설정
│   └── src/
│       ├── app/                         ← 앱 셸 (MaterialApp)
│       │   └── synced_alarm_app.dart
│       ├── models/                      ← 데이터 모델
│       │   ├── alarm.dart               ← Alarm 모델 (이름, 시간, 요일, 설정 등)
│       │   ├── account.dart             ← Account, SyncGroup 모델
│       │   └── device_registration.dart ← DeviceRegistration 모델
│       ├── data/                        ← 데이터 접근 계층 (Repository 패턴)
│       │   ├── alarm_repository.dart         ← AlarmRepository 인터페이스
│       │   ├── account_repository.dart       ← AccountRepository 인터페이스
│       │   ├── local_demo_alarm_repository.dart  ← 로컬 저장소 구현
│       │   ├── firebase_alarm_repository.dart    ← Firebase 저장소 구현
│       │   ├── firebase_account_repository.dart  ← Firebase 계정 구현
│       │   ├── firebase_device_registrar.dart    ← FCM 기기 등록/해제
│       │   ├── firebase_operation_timeout.dart   ← Firestore 타임아웃 보호
│       │   └── app_providers.dart                ← Riverpod Provider 정의
│       ├── features/                    ← 화면/기능 UI
│       │   ├── alarms/
│       │   │   ├── alarm_home_screen.dart     ← 메인 홈 화면 (알람 목록)
│       │   │   ├── alarm_editor_sheet.dart    ← 알람 생성/수정 바텀시트
│       │   │   ├── alarm_ring_screen.dart     ← 알람 울림 화면 (해제/스누즈)
│       │   │   ├── alarm_due_tick_tracker.dart ← 알람 트리거 추적
│       │   │   └── permission_guide_screen.dart ← 권한 안내 화면
│       │   ├── account/
│       │   │   ├── auth_screen.dart            ← 로그인/회원가입 화면
│       │   │   ├── account_gate.dart           ← 인증 게이트
│       │   │   ├── account_settings_screen.dart← 계정 설정 화면
│       │   │   └── group_setup_screen.dart     ← 그룹 생성/참가/관리 화면
│       │   └── settings/
│       │       └── settings_sheet.dart         ← 설정 화면 (테마, 언어, 앱 정보)
│       ├── design/                      ← 디자인 시스템
│       │   ├── app_theme.dart           ← Stitch 기반 그린 테마 토큰
│       │   └── app_localizations.dart   ← 한국어/영어 다국어 지원
│       └── platform/                    ← 플랫폼 연동 계층
│           ├── alarm_notification_service.dart ← 알림/FCM/스케줄링 서비스
│           └── alarm_task_controller.dart      ← 네이티브 채널 제어
├── android/app/src/main/kotlin/         ← Android 네이티브 (Kotlin)
│   ├── LaunchRouterActivity.kt          ← 앱 실행/알람 실행 분기
│   ├── MainActivity.kt                 ← 일반 앱 메인 Activity
│   ├── AlarmActivity.kt                ← 잠금화면 전용 알람 Activity
│   ├── SyncedAlarmFlutterActivity.kt    ← Flutter-네이티브 브릿지
│   ├── ForegroundAlarmReceiver.kt       ← 포그라운드 알람 수신기
│   ├── ForegroundAlarmScheduler.kt      ← AlarmManager 스케줄링
│   ├── AlarmVibrationController.kt      ← 네이티브 진동 패턴 제어
│   └── LaunchRouteDecision.kt           ← 실행 경로 판별
├── functions/                           ← Cloud Functions (Node.js)
│   └── index.js                         ← 5개 함수: createGroup, joinGroup,
│                                           onAlarmWrite, onCommandCreate,
│                                           cleanupInvalidTokens
├── firestore.rules                      ← Firestore 보안 규칙
└── test/                                ← 자동화 테스트 (7개 파일, 47개 케이스)
```

### 2-5. 주요 클래스 흐름 설명

#### 알람 생성 흐름
```
사용자 입력 → AlarmEditorSheet (UI)
  → AlarmListController (상태 관리)
    → AlarmRepository.addAlarm() (데이터 저장)
      ├── [로컬 모드] LocalDemoAlarmRepository → SharedPreferences
      └── [Firebase 모드] FirebaseAlarmRepository → Firestore
        → Cloud Functions (onAlarmWrite) → FCM → 다른 기기
```

#### 알람 울림 흐름
```
Android AlarmManager → ForegroundAlarmReceiver (Kotlin)
  → Flutter MethodChannel
    → AlarmNotificationService (알림 표시)
      → AlarmHomeScreen → AlarmRingScreen (울림 UI)
        → 사용자: Dismiss 또는 Snooze
          → Firestore 상태 업데이트
            → Cloud Functions (onCommandCreate)
              → FCM → 다른 기기의 AlarmRingScreen 닫힘
```

#### 멀티 디바이스 동기화 흐름
```
기기 A: 알람 생성/수정/삭제
  → Firestore 저장
    → Cloud Functions (onAlarmWrite)
      → FCM 데이터 메시지 전송 (무소음 Silent Sync)
        → 기기 B (백그라운드/포그라운드)
          → AlarmNotificationService.showRemoteMessage()
            → FCM 페이로드에서 알람 속성 직접 파싱
              → Android 로컬 알람 즉시 재스케줄링
                (Firestore GET 없이 오프라인에서도 동작)
```

---

## (3) 주요 기능 설명 (10점)

### 3-1. 기본 구현 기능 (Android) — 전체 완성

| 기능 | 설명 | 구현 상태 |
|---|---|---|
| **알람 목록 표시** | 홈 화면에서 모든 알람을 카드 형태로 표시, 그룹 뱃지 포함 | ✅ 완료 |
| **알람 생성** | 바텀시트에서 이름, 시간, 요일, 소리/진동/스누즈 설정 | ✅ 완료 |
| **알람 수정** | 기존 알람 탭하여 설정 편집, revision 증가로 버전 충돌 방지 | ✅ 완료 |
| **알람 삭제** | 스와이프 또는 편집 화면에서 삭제 | ✅ 완료 |
| **알람 활성화/비활성화** | 토글 스위치로 on/off, 비활성 시 예약 해제 | ✅ 완료 |
| **알람 울림 알림** | Full-screen intent, 잠금화면 표시, 화면 자동 켜기 | ✅ 완료 |
| **알람 해제 (Dismiss)** | 앱 내부 + 시스템 알림 액션 버튼 + 잠금화면 전용 Activity | ✅ 완료 |
| **스누즈 (Snooze)** | 설정된 간격/횟수만큼 재알림, Ongoing 알림으로 상태 표시 | ✅ 완료 |
| **알람 동기화** | Firebase 기반 실시간 동기화 + FCM 백그라운드 무소음 동기화 | ✅ 완료 |

### 3-2. 확장 구현 기능 — 구현 완료 항목

| 기능 | 설명 | 구현 상태 |
|---|---|---|
| **이메일 계정 관리** | Email/Password 회원가입, 로그인, 로그아웃 | ✅ 완료 |
| **그룹 관리** | 그룹 생성, 초대 코드 참가, 그룹 전환 | ✅ 완료 |
| **다중 그룹 알람 통합** | 가입한 모든 그룹의 알람을 하나의 목록에 통합 표시 | ✅ 완료 |
| **멀티 디바이스 동기화** | 같은 그룹의 여러 기기가 실시간으로 알람 공유 | ✅ 완료 |
| **원격 해제/스누즈 동기화** | 한 기기에서 해제 시 다른 기기 울림 화면 즉시 닫힘 | ✅ 완료 |
| **테마 변경** | 라이트/다크/시스템 모드 전환, SharedPreferences 영속 | ✅ 완료 |
| **한국어/영어 전환** | 인앱 언어 변경 지원 | ✅ 완료 |
| **기기 관리** | 등록 기기 목록 조회, 다른 기기 해제 | ✅ 완료 |
| **보안** | Firestore 규칙 기반 그룹 멤버 접근 제어, 초대 코드 해시 저장 | ✅ 완료 |
| **소리/진동 개별 설정** | 알람별 소리 on/off, 진동 on/off | ✅ 완료 |
| **로컬 모드** | Firebase 없이도 알람 사용 가능 (SharedPreferences 기반 영속) | ✅ 완료 |

### 3-3. 각 화면별 UI 기능 설명

#### ① 홈 화면 (AlarmHomeScreen)
- **3개 하단 탭**: 알람 목록 / 기록 / 설정
- 알람 카드: 시간, 이름, 요일, 활성 토글, 그룹 뱃지 표시
- 스누즈 중인 알람: 카드에 스누즈 횟수/예정 시각 뱃지 + 해제 기능
- 우상단: 동기화 상태 아이콘 (로그인 여부, 그룹 연결 상태)
- FAB(+) 버튼으로 새 알람 생성

#### ② 알람 편집 바텀시트 (AlarmEditorSheet)
- 알람 이름, 시간(TimePicker), 요일 반복(월~일 칩)
- 울림 지속 시간, 소리 on/off, 진동 on/off
- 다시 알림(스누즈) 사용 on/off, 스누즈 간격, 스누즈 최대 횟수
- Firebase 모드: 그룹 선택 드롭다운

#### ③ 알람 울림 화면 (AlarmRingScreen)
- 전체 화면 알람 표시 (잠금화면에서도 동작)
- Dismiss(해제) / Snooze(다시 알림) 버튼
- 울림 지속 시간 만료 시 자동 스누즈 (또는 자동 해제)
- 다른 기기에서 해제 시 자동으로 화면과 진동 종료

#### ④ 로그인/회원가입 화면 (AuthScreen)
- 이메일/비밀번호 입력 폼
- 로그인/회원가입 전환 토글
- Firebase 인증 오류 시 한글 피드백 (예: "이메일 형식이 올바르지 않습니다")

#### ⑤ 그룹 관리 화면 (GroupSetupScreen)
- 그룹 생성 (이름 + 초대 코드 자동 생성)
- 초대 코드 입력으로 그룹 참가
- 그룹 목록 + 정보 아이콘 → 멤버/기기 리스트 팝업

#### ⑥ 계정 설정 화면 (AccountSettingsScreen)
- 로그인 상태 표시 (이메일, UID)
- 등록 기기 목록 (현재 기기 뱃지, 다른 기기 해제 가능)
- 로그아웃 버튼

#### ⑦ 설정 화면 (SettingsSheet)
- 테마 설정 (라이트/다크/시스템)
- 언어 설정 (한국어/English)
- 앱 정보 (버전 표시)
- 기본 알람 설정

### 3-4. 기술적 구현 난이도가 높은 포인트

1. **백그라운드 FCM 수신 시 Firestore 없이 즉시 로컬 알람 재스케줄링**
   - OS 절전 모드/네트워크 차단 환경에서도 FCM 데이터 페이로드만으로 알람 속성 복원

2. **Kotlin 네이티브 AlarmManager + Flutter 브릿지**
   - LaunchRouterActivity → 일반 실행 / 잠금화면 알람 분기
   - ForegroundAlarmReceiver로 앱 포그라운드 상태에서도 정시 트리거

3. **멀티 디바이스 Dismiss/Snooze 명령 동기화**
   - alarm.command 페이로드로 다른 기기의 울림 화면 즉시 정리
   - alarm.updated와의 순서 경쟁 방지 설계

4. **알람 재울림 방지 (DueTickTracker)**
   - Sliding Window 방식의 12시간 주기 틱 정리
   - SharedPreferences에 처리 상태 영속 저장 + Activity 간 공유

5. **Firestore 보안 규칙**
   - 그룹 멤버만 알람/기기 데이터 접근 가능
   - 초대 코드 해시를 별도 컬렉션에 저장하여 클라이언트 읽기 차단

---

## (4) 프로그램 시연 시나리오 (5점)

> 아래 순서대로 시연하면 기본 구현 + 확장 구현의 핵심 기능을 모두 보여줄 수 있습니다.

### 시나리오 1: 로컬 모드 기본 알람 (약 2분)
1. 앱 첫 실행 → 빈 알람 목록 화면 확인
2. FAB(+) 탭 → 알람 이름 입력, 시간 설정, 요일 선택
3. 소리/진동/스누즈 설정 변경 후 저장
4. 알람 카드가 목록에 추가됨 확인
5. 토글로 비활성화 → 다시 활성화
6. 알람 탭하여 편집 → 시간 변경 후 저장
7. 스와이프로 삭제

### 시나리오 2: 알람 울림 및 제어 (약 2분)
1. 1~2분 뒤로 알람 설정
2. 알람 시간 도달 → **전체 화면 알람 울림** (소리 + 진동)
3. **Snooze** 탭 → 상단바에 Ongoing 스누즈 알림 표시
4. 스누즈 시간 후 재울림 → **Dismiss** 탭 → 알림 소멸

### 시나리오 3: 계정 로그인 및 그룹 생성 (약 2분)
1. 설정 탭 → 계정 설정 → 회원가입 (이메일/비밀번호)
2. 로그인 후 → 그룹 생성 (그룹 이름 입력)
3. 초대 코드 복사
4. 그룹 정보 아이콘 탭 → 멤버/기기 리스트 확인

### 시나리오 4: 멀티 디바이스 동기화 (약 3분) ⭐핵심 시연
1. **기기 B**에서 같은 계정으로 로그인 → 초대 코드로 그룹 참가
2. **기기 A**에서 알람 생성 → **기기 B**에 실시간 반영 확인
3. **기기 A**에서 알람 수정 → **기기 B**에 즉시 동기화 확인
4. 알람 시간 도달 → **양쪽 기기 동시 울림**
5. **기기 A**에서 Dismiss → **기기 B**의 알람 화면이 **즉시 닫힘** ⭐

### 시나리오 5: 설정 및 부가 기능 (약 1분)
1. 다크 모드 전환 확인
2. 한국어 ↔ 영어 전환 확인

---

## 참고: 발표 슬라이드 구성 제안

| 슬라이드 | 내용 | 시간 |
|---|---|---|
| 1 | 표지 (앱 이름, 팀명, 팀원) | 0.5분 |
| 2 | 개발 배경 및 프로그램 개요 | 1분 |
| 3 | 기술 스택 및 팀원 역할 분담 | 1분 |
| 4-5 | 전체 시스템 구조도 + 동기화 흐름 | 2분 |
| 6 | Firestore 데이터 구조 + 보안 | 1분 |
| 7 | 앱 내부 코드 구조 (레이어드 아키텍처) | 1분 |
| 8-10 | 주요 기능 설명 (화면별 UI 캡처 포함) | 2분 |
| 11 | 기술적 난이도 포인트 | 1分 |
| 12-14 | **라이브 시연** | 3분 |
| 15 | 향후 계획 + Q&A | 1분 |

---

## 참고: 추가 정보

### 디자인 컬러 시스템 (Stitch 기반 그린 테마)
- Primary: `#386948`
- Primary Container: `#b9efc5`
- Background: `#f7faf4`
- Surface: `#ffffff`
- Text: `#2c342e`
- Card Radius: 12px~16px

### Cloud Functions 목록
| 함수명 | 트리거 | 역할 |
|---|---|---|
| `createGroup` | HTTPS Callable | 그룹 생성 + 초대 코드 해시 저장 |
| `joinGroup` | HTTPS Callable | 초대 코드 검증 + 그룹 참가 |
| `onAlarmWrite` | Firestore 트리거 | 알람 변경 시 그룹 기기에 FCM 전송 |
| `onCommandCreate` | Firestore 트리거 | 해제/스누즈 명령 시 FCM 전송 |
| `cleanupInvalidTokens` | 스케줄/수동 | 유효하지 않은 FCM 토큰 정리 |

### 자동화 테스트 목록
| 테스트 파일 | 주요 검증 내용 |
|---|---|
| `widget_test.dart` | 알람 목록, 생성/수정, 울림 화면, 원격 dismiss, 스누즈, 설정 |
| `account_flow_test.dart` | 로컬 데모 → 로그인 → 그룹 생성/참가 흐름 |
| `alarm_model_test.dart` | 알람 모델 파싱, 직렬화, 요일 계산 |
| `alarm_notification_service_test.dart` | 알림 채널, FCM 수신, 액션 처리 |
| `alarm_due_tick_tracker_test.dart` | Sliding Window 틱 추적 |
| `device_registration_test.dart` | 보안 저장소 마이그레이션, 기기 등록 |
| `local_demo_alarm_repository_test.dart` | 로컬 영속 저장, 정렬, 레거시 정리 |
